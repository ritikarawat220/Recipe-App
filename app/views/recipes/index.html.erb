<section class="my-3 p-3">
  <% @recipes.each do |recipe| %>
    <% unless recipe.status == 'private' %>
      <%= render 'recipe', recipe: recipe %>
    <% end %>
  <% end %>

  <%= link_to 'New Recipe', new_recipe_path, class: 'btn btn-outline-dark btn-sm' %>
</section>
